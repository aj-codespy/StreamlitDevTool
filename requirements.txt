# requirements.txt for the Streamlit App Generator

# Core Streamlit framework
streamlit>=1.33.0,<2.0.0

# Google Generative AI SDK for Gemini API
google-generativeai>=0.5.0,<0.7.0

# Streamlit component for the ACE code editor
streamlit-ace>=0.1.1,<0.2.0

# Optional: For improved clipboard functionality if you were to implement
# a more robust server-side copy mechanism (though the current app uses JS for client-side copy)
# pyperclip>=1.8.2,<2.0.0
